/* =========================================================
   ZACK PORTFOLIO
   PROJECTS
   ========================================================= */

/* =========================================================
   INIT PROJECTS
   ========================================================= */

function initProjects() {
  const projectsContainer = document.querySelector("[data-projects]");

  if (!projectsContainer) {
    return;
  }

  /*
   * Projects are currently rendered
   * directly from HTML.
   *
   * No JSON required.
   */

  console.log("Projects initialized.");
}
