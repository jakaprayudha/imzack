/* =========================================================
   ZACK PORTFOLIO
   Main Application
   ========================================================= */

document.addEventListener("DOMContentLoaded", () => {
  console.log("Zack Portfolio initialized.");

  initNavbar();
  initAnimations();
  initCounters();
  initProjects();
  initProjectFilter();
  initContactForm();
});
